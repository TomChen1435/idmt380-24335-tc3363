# New Features Added

- Accessible Fonts
    - Use Open Sans, a sans-serif font, for all text elements
    - Avoid the use of italics at all times
- Font Size & Scaling + Line Height & Spacing + Responsive Typography + CSS clamp() Function
    - Set all line-height to 1.5
    - For mobile devices (320px <= width < 744px)
        - Set paragraph font-size to "clamp(1rem, 5vw, 1.25rem)"
        - Set h1 font-size to "clamp(1.75rem, 8.75vw, 2rem)"
        - Set h2 & h3 font-size to "clamp(1.5rem, 7.5vw, 1.75rem)"
    - For tablets (744px <= width < 1024px)
        - Set paragraph font-size to "clamp(1.125rem, 2.35vw, 1.375rem)"
        - Set h1 font-size to "clamp(2.25rem, 4.75vw, 2.5rem)"
        - Set h2 & h3 font-size to "clamp(1.75rem, 3.65vw, 2rem)"
    - For desktop devices (1024px <= width)
        - Set paragraph font-size to "clamp(1.25rem, 1.85vw, 1.5rem)"
        - Set h1 font-size to "clamp(3rem, 4.65vw, 3.5rem)"
        - Set h2 & h3 font-size to "clamp(2.5rem, 3.85vw, 3rem)"
- Line Length
    - Set max-width for all paragraph elements to 68ch
- Color Contrast
    - All text, anchor, and button elements use colors that comply with WCAG 2.1 AA Standards against the background (contrast >= 4.5:1)
- Color Blindness Considerations + Keyboard Navigation & Focus
    - Use a vertical offset to indicate links' hover state
- Text Alignment
    - Avoid the use of justified texts for all text elements
- Screen Reader Considerations
    - Use semantic HTML tags like "article"
    - Use descriptive texts for all links
    - Verify alt text for images
    - Did not include aria-label as there is no need to use this attribute on this page
- Skip Navigation Link
    - Did not include a skip navigation link as there is no need to use one on this page
- Respect User Preferences
    - Use "prefers-reduced-motion" and "prefers-color-scheme" media queries
